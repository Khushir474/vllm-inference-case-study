# Setup Complexity Log

"Setup complexity" isn't something a script can measure — log it by hand for each
backend as you set it up. Copy this template once per backend.

## Backend: <hosted | vllm>

- Date:
- Provider / instance type (if self-hosted):
- Time from starting signup/provisioning to first successful API call:
- Number of manual config steps required (account signup, API key, env vars, model
  download, server flags, etc.) — list them:
- Errors or surprises hit along the way:
- Anything that required reading external docs beyond this repo's README:
- Overall friction (1-5, 5 = painful):
